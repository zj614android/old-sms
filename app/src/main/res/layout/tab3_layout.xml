<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/lv_bg"
    android:padding="10dp" >

    <TextView
        android:id="@+id/textView1"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/common_edittext_bg_normal"
        android:ellipsize="marquee"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:singleLine="true"
        android:text="若想更改或是删除群组请长按您所想编辑的群组条目即可，感谢对本软件的支持。" />

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:orientation="horizontal" >

        <Button
            android:id="@+id/tab3_left_bt"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/selector_btn"
            android:text="添加群组" />
    </LinearLayout>

    <ListView
        android:id="@+id/tab3_lv"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:background="@drawable/common_edittext_bg_normal"
        android:divider="@android:color/transparent"
        android:listSelector="@android:color/transparent"
        android:visibility="visible" >
    </ListView>

    <ImageView
        android:id="@+id/tab3_nullimg"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:src="@drawable/zanwuqunzu"
        android:visibility="gone"
        android:background="@drawable/common_edittext_bg_normal" />

</LinearLayout>